// block_test.cpp
/*#include "blockchain.hpp"
#include <iostream>

int main11() {
    Blockchain myChain;

    // Add some votes
    myChain.addBlock("Vote: Alice -> Bob");
    myChain.addBlock("Vote: Charlie -> Dave");
    myChain.addBlock("Vote: Eve -> Mallory");

    std::cout << "\n--- Blockchain Blocks ---" << std::endl;
    myChain.printChain();

    std::cout << "\nBlockchain valid? " << (myChain.isChainValid() ? " Yes" : " No") << std::endl;

    return 0;
}
*/